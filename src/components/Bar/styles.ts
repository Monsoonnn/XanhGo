import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    barItem: { alignItems: 'center' },
    bar: { width: 18, borderRadius: 4, marginBottom: 6 },
    barLabel: { fontSize: 12, color: '#333' },
});
