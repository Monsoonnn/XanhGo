import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    wrap: {
        width: '100%',
        height: '100%'
    },
    video: {
        width: '100%',
        height: '100%'
    }
});
// styles for TreeVideo component

export default styles;
